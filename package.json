{
  "name": "agenda",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "demo": "node demo-server.js",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "node server.js",
    "client": "cd client && npm start",
    "build": "cd client && CI=false npm run build",
    "postbuild": "echo \"React app built successfully\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@hapi/hapi": "^21.3.2",
    "@hapi/inert": "^7.1.0",
    "@hokify/agenda": "^6.3.0",
    "agendash": "^4.0.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
